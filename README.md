# eventagent
This repo is for building event planner agent app

## Project Source of Truth

The persistent project memory lives in [`event-planner-agent/source-of-truth`](event-planner-agent/source-of-truth).
Use it to capture product direction, decisions, backlog, and session notes so future work can resume with context.
